<% provide(:title, 'New Building') %>
<h1>Setup a New Building</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@building) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Install New Building", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>