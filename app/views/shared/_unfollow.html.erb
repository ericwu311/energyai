<%= form_for(current_user.relationships.find_by_followed_type_and_followed_id(followed.class.name, followed.id), html: {method: :delete }, 
	remote: true) do |f| %>
	<%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>