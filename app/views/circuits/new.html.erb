<%= form_for [@bud, @circuit] do |f| %>
  <%= render 'shared/error_messages', object: @circuit %>
  	<%= f.label :name %>
    <%= f.text_field :name %>
    Side: 0 (left) or 1 (right)
    <%= f.text_field :side %>
    Location: id format: x.y
    <%= f.text_field :location %>
  <%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>