<% unless current_user?(@user) %>
	<div id="follow_form">
	<% if current_user.following?(@user) %>
		<%= render 'shared/unfollow', followed: @user %>
	<% else %>
		<%= render 'shared/follow', followed: @user %>
	<% end %>
	</div>
<% end %>  